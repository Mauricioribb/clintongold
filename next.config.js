/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['picsum.photos', 'clintongold.com.br', 'www.transparenttextures.com'],
  },
};

module.exports = nextConfig;
